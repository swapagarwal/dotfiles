- clean: ['~']

- link:
    ~/.dotfiles: ''
    ~/.aliases: aliases
    ~/.functions: functions

- shell:
  - [chmod +x setup]
  -
    command: "./setup"
    stdout: true
    stderr: true
    stdin: true
  - [git submodule update --init --remote, Updating submodules]
