- clean: ['~']

- link:
    ~/.dotfiles: ''
    ~/.aliases: aliases

- shell:
  - [grep -xq ". ~/.aliases" ~/.bashrc || printf "\n. ~/.aliases\n" >> ~/.bashrc]
  - [git submodule update --init --remote, Updating submodules]
